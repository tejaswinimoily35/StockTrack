pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build') {
            steps {
                echo 'Build stage executed (dummy)'
            }
        }

        stage('Test') {
            steps {
                echo 'Test stage executed (dummy)'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy stage executed (dummy)'
            }
        }

    }
}
